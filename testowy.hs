import DensePoly
import SparsePoly
import Representation
import PolyClass

type DPI = DensePoly Int
type SPI = SparsePoly Int

main = print(res)

res = qrP (S [(2, 1), (1, -1), (0, -6)]) (S [(1, 1), (0, 2)])
